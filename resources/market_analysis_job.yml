resources:
  jobs:
    market_analysis:
      name: "Market Analysis - Average Yield by City"
      
      tasks:
        - task_key: run_market_analysis
          sql_task:
            warehouse_id: ${var.warehouse_id}
            file:
              path: ../queries/market_analysis.sql
          
          timeout_seconds: 300
      
      